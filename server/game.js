var deck = require('./deck');
var player = require('./player');

function Game () {
  this.players = 0;
  this.playerWinning = playerWinning;
}

Game.prototype.game = function() {
  // turn till cards gone
  // var deck = new Deck();
};

Game.prototype.turn = function() {
  //  player input ask other player
  //  if other player hand index of ask
    //  other player deck splice and game deck splice other player new card
    //  current player deck splice matching card and current player point and new card deck splice
    //  current player turn again
  // else
    // next players turn
};

module.exports = {};


//  get num players
//  deal 7 cards to player
